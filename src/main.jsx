import React from "react";
import ReactDOM from "react-dom/client";
import { TorneoPokemon } from "./torneoPokemon";

ReactDOM.createRoot(document.getElementById("root")).render(
  // <React.StrictMode>
  <TorneoPokemon />
  // </React.StrictMode>
);
